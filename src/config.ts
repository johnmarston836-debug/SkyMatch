/**
 * Where "Write to the developer" in Settings sends its email. Empty until
 * there is an address: the row then says it is coming soon instead of
 * opening a mail app with nobody to write to.
 */
export const DEVELOPER_EMAIL = '';
